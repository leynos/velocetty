/**
 * @file App-runtime re-exports for golden path runtime plugin definitions.
 *
 * Keeps app and renderer/runtime consumers aligned on a single shared source
 * of truth for manifest metadata, settings defaults, and runtime types.
 */
export * from '@shared/runtime/golden-path-demo';
