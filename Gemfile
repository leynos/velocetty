# frozen_string_literal: true

source 'https://rubygems.org'

# FPM is a Ruby gem for building rpm, deb, and pacman packages
# Pinned to 1.15.1 for reproducible builds; update periodically
gem 'fpm', '1.15.1'
